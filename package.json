{
  "name": "svelte-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "astro build",
    "deploy": "npm run build && npx gh-pages -d dist/ -t true",
    "dev": "astro dev"
  },
  "devDependencies": {
    "astro": "^0.21.13",
    "svelte-three": "^0.1.2"
  },
  "dependencies": {
    "@astrojs/renderer-svelte": "^0.2.3",
    "sveltestrap": "^5.6.3"
  }
}
